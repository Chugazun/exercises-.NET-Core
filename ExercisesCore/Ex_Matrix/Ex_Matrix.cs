using System;
using System.Collections.Generic;
using System.Text;

namespace ExercisesCore.Ex_Matrix
{
    class Ex_Matrix
    {
        //static void Main(string[] args)
        //{
        //    Console.Write("Insert the Matrix dimentions (NxN): ");
        //    int n = int.Parse(Console.ReadLine()), oddCount = 0;
        //    int[,] mat = new int[n, n];
        //    string mainDiag = "";

        //    Console.WriteLine("Insert the Matrix elements");

        //    for (int i = 0; i < mat.GetLength(0); i++)
        //    {
        //        for (int j = 0; j < mat.GetLength(1); j++)
        //        {
        //            Console.Write($"({i}, {j}) = ");
        //            mat[i, j] = int.Parse(Console.ReadLine());
        //            if (i == j) mainDiag += mat[i, j] + " ";
        //            if (mat[i, j] < 0) oddCount++;
        //        }
        //    }
        //    Console.WriteLine($"---------------------------------\nMain Diagonal: {mainDiag}\nNegative Numbers: {oddCount}");
        //}
        
    }
}
